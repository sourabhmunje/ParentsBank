@model IEnumerable<ParentsBank.Models.Transaction>

<h2> Transactions </h2>

<p>
    @Html.ActionLink("Create New", "Create", "Transactions")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Owner)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransactionDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Note)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Owner)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransactionDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Note)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", "Transactions", new { id=item.ID }, new { }) |
            @Html.ActionLink("Details", "Details", "Transactions" ,new { id=item.ID }, new { }) |
            @Html.ActionLink("Delete", "Delete", "Transactions", new { id=item.ID }, new { })
        </td>
    </tr>
}

</table>
